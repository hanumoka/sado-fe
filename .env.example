# SADO FE 환경 변수
# 이 파일을 복사하여 .env 파일을 생성하세요

# ============================================================
# 개발 환경 설정 (Vite dev server)
# ============================================================

# Vite 프록시 타겟 - 개발 서버에서 /dicomweb, /api 요청을 전달할 백엔드 주소
# 백엔드가 다른 IP에서 실행되는 경우 이 값을 변경하세요
VITE_API_TARGET=http://localhost:10201

# ============================================================
# 프로덕션 환경 설정 (빌드 후)
# ============================================================

# 프로덕션에서는 상대 경로(/dicomweb, /api)를 사용하므로
# Nginx 등 리버스 프록시에서 백엔드로 라우팅 설정이 필요합니다.
#
# Nginx 설정 예시:
# location /dicomweb/ {
#     proxy_pass http://backend-server:10201/dicomweb/;
# }
# location /api/ {
#     proxy_pass http://backend-server:10201/api/;
# }
